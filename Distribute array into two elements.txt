class Solution {
public:
    vector<int> resultArray(vector<int>& nums) {
        int n=nums.size();
        vector<int>arr1;
        vector<int>arr2;
        arr1.reserve(n);
        arr2.reserve(n);
        arr1.push_bak(nums[0]);
        arr2.push_backc(nums[1]);
        vector<int>ans;
        for(int i=2;i<nums.size();i++){
            if(arr1.back()>arr2.back()){
                arr1.push_back(nums[i]);
            }
            else{
                arr2.push_back(nums[i]);
            }
        }
        // int i=0;
        // int j=0;
        // while(i<arr1.size()){
        //     ans.push_back(arr1[i]);
        //     i++;
        // }
        // while(j<arr2.size()){
        //     ans.push_back(arr2[j]);
        //     j++;
        // }
        // return ans;

        arr1.insert(arr1.end(),arr2.begin(),arr2.end());
        return arr1;
    }
};