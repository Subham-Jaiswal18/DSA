class Solution {
public:
    void moveZeroes(vector<int>& nums) {

        int start=0;                                     //Move non zero values to the start

        for(int i=0;i<nums.size();i++){

            if(nums[i]!=0){

                swap(nums[i],nums[start]);

                start++;
            }
        }
    }
};